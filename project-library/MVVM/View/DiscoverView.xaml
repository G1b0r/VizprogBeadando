<UserControl x:Class="project_library.MVVM.View.DiscoverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:project_library"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Discover" Foreground="#fcf6f5" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="35" Margin="20,15,15,15"/>

        <!-- Scrollable List of Books -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding UniqueBooks}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Top" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.SelectBookCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          CommandParameter="{Binding}"
                          Background="Transparent"
                            BorderBrush="Transparent"
                            Cursor="Hand"
                            Padding="0"
                            Width="200"
                            Height="250"
                            Margin="10">
                            <Button.ToolTip>
                                <ToolTip Placement="Right" HorizontalOffset="10" VerticalOffset="0">
                                    <StackPanel>
                                        <!-- Book Title -->
                                        <TextBlock Text="{Binding title}" FontWeight="Bold" FontSize="16" Foreground="Black" />
                                    </StackPanel>
                                </ToolTip>
                            </Button.ToolTip>
                            <Image Source="{Binding pic_name}" Width="200" Height="250" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                            </Button>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
