<UserControl x:Class="project_library.MVVM.View.BookDetailView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:local="clr-namespace:project_library"
            mc:Ignorable="d"
            d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <!-- vissz + konyvcover-->
        <StackPanel>
            <Button x:Name="backBtn" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,10,20" Content="← Go back" Width="180" FontSize="17" Command="{Binding takajodj}"/>
            <Border Width="250" Height="350" CornerRadius="5" Margin="5">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="3"/>
                </Border.Effect>
                <Border.Background>
                    <ImageBrush ImageSource="https://m.media-amazon.com/images/I/71-++hbbERL._AC_UF894,1000_QL80_.jpg"/>
                </Border.Background>
            </Border>
        </StackPanel>
        <!-- cim szerzo + leiras-->
        <StackPanel Grid.Column="1" Margin="5">
            <TextBlock Margin="5 10 10 5" Text="Harry potter és az apja fasza" FontSize="27" Foreground="White" FontFamily="Comic Sans MS"/>
            <TextBlock Margin="20 0 0 0" Text="Horror" FontSize="20" Foreground="White">
                <TextBlock.RenderTransform>
                    <SkewTransform AngleX="-10"/>
                </TextBlock.RenderTransform>
            </TextBlock>
            <!--  -->
            <ItemsControl x:Name="szerzok" ItemsSource="{Binding szerzo_lista}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Width="175" Height="280">
                            <StackPanel>
                                <TextBlock Margin="5" Text="{Binding szerzo_name}" Foreground="white" FontSize="25"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <!--  -->
            <AccessText Margin="5 10 10 5" TextWrapping="WrapWithOverflow" Foreground="White" FontSize="12" Text="iviuwiu fe is hsei sjsd isibis sd isdjsd isjsd isej isj s shj vsjsvbjjsdjnvsnkssv s svdi svd hsvd  apiofdvniaebv aeiprvbuaid aipdvbe waldo iuubsavboivs iupavbv oibavuivda viobsavbiva"/>
        </StackPanel>
        <!-- parancsok es egyeb dolgok -->
        <StackPanel Grid.Column="2">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="Available   pieces:" Foreground="White" Margin="5" TextWrapping="Wrap" Width="60" HorizontalAlignment="Left"/>
                <!--ide kell egy bind-->
                <TextBlock Text="bind" Foreground="White" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5"/>
            </StackPanel>
            <!-- ha vett ki a user akk ide a visszahozni hatarido-->
            <StackPanel Visibility="{Binding IsTakenByUser}">
                <TextBlock Text="End of lease time:" Foreground="White" Margin="5" FontSize="13" TextWrapping="WrapWithOverflow" TextAlignment="Right"/>
                <TextBlock Text="2025.04.26" FontSize="13" Foreground="White"/>
            </StackPanel>
            <StackPanel Visibility="{Binding IstakenByUserInvert}">
                <Button Command="{Binding takeBookOut}" Content="Take out" Width="90" Margin="5"/>
            </StackPanel>
            <!--admin features -->
            <StackPanel Visibility="{Binding IsAdmin}">
                <Button Visibility="{Binding Isdeleted}" Command="{Binding makeitVisible}" x:Name="makeVisible" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5" Content="Enable" Width="90" FontSize="13" />
                <Button Visibility="{Binding IsdeletedInvert}" Command="{Binding makeitInisible}" x:Name="makeInvisible" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5" Content="Disable" Width="90" FontSize="13" />

                <Button x:Name="changeAmmount" HorizontalAlignment="Center" Content="Ammount" Width="90" Margin="5" Click="changeAmmount_Click"/> 
            </StackPanel>
        </StackPanel>
        
    </Grid>
</UserControl>
