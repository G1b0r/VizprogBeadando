<UserControl x:Class="project_library.MVVM.View.HomeView"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:local="clr-namespace:project_library"
            mc:Ignorable="d"
            d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Home" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="35" Margin="20,20,20,20"/>
        <TextBlock Text="This week's top picks:" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="20" Margin="20,0,0,0" Grid.Row="1"/>

        <ItemsControl ItemsSource="{Binding TopBooks}" Grid.Row="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="10">
                        <!-- Button with Image -->
                        <Button x:Name="ImageButton" Command="{Binding DataContext.SelectBookCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
         CommandParameter="{Binding}" Background="Transparent" BorderBrush="Transparent" Cursor="Hand" Padding="0"  Width="{Binding DataContext.ButtonWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
 Height="{Binding DataContext.ButtonHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                            <Image Source="{Binding pic_name}" Width="200" Height="250" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                        </Button>
                        <Button Command="{Binding DataContext.SelectBookCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
        CommandParameter="{Binding}" Background="Transparent" BorderBrush="Transparent" Cursor="Hand" Padding="5,2,5,2" Margin="0,10,0,0">
                            <TextBlock Text="{Binding title}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" />
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
